# Guidelines for AI Agents

This repository is the Tier A IDD experiment for the `daymark` offline
task-planning CLI. Keep the experiment protocol and loop observations in
[`docs/tier-a-experiment.md`](docs/tier-a-experiment.md).

See `AGENTS.md` for shared repository rules and validation commands.

## IDD workflow

Use [docs/idd-workflow.md](docs/idd-workflow.md) as the entry path.
Before each phase, read
`.github/instructions/idd-overview-core.instructions.md` and the routed
phase instruction. This experiment uses `mid-llm-test` markers,
`instructions-only` helpers, and a `no-advisory` review profile.
